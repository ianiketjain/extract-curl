{
  "name": "curl-extract-build",
  "version": "1.0.0",
  "description": "The package to paste any curl and get extracted data from the curl. also we can create a curl by following provided inputs. with support of terminal run ",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified here\" && exit 1",
    "start": "ts-node index.ts",
    "build": "tsc",
    "getfun": "ts-node index.ts"
  },
  "keywords": [
    "curl",
    "make",
    "extract",
    "extract-curl",
    "make-curl",
    "postman",
    "api",
    "build"
  ],
  "author": "Aniket Mulkutkar",
  "license": "ISC",
  "dependencies": {
    "ts-node": "^10.0.0",
    "typescript": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.2"
  }
}
